# Design

This directory contains technical design documents and implementation status records.

Design documents should include status when applicable:

- `Draft`
- `Approved`
- `Implemented`

Legacy feature documents may still exist in older `docs/` subdirectories. Move or link them here when they are next edited.
